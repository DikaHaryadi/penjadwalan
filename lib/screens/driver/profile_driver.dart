import 'package:flutter/material.dart';

class ProfileDriver extends StatelessWidget {
  const ProfileDriver({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ini profile driver'),
    );
  }
}
